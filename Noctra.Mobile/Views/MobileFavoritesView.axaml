<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:Noctra.ViewModels;assembly=Noctra.Core"
             xmlns:models="using:Noctra.Models"
             xmlns:loc="using:Noctra.Mobile.Localization"
             xmlns:controls="using:Noctra.Mobile.Controls"
             x:Class="Noctra.Mobile.Views.MobileFavoritesView"
             x:DataType="vm:MainViewModel"
             x:CompileBindings="False"
             mc:Ignorable="d"
             d:DesignWidth="390"
             d:DesignHeight="720">
  <Grid Margin="20,16,20,28"
        RowDefinitions="Auto,*"
        RowSpacing="16">
    <Grid Grid.Row="0" ColumnDefinitions="*,Auto">
      <TextBlock Text="{loc:Translate Favorites.Title}"
                 FontSize="28"
                 FontWeight="Bold"
                 Foreground="{DynamicResource TextPrimaryBrush}"
                 VerticalAlignment="Center" />
      <Border Grid.Column="1"
              Background="{DynamicResource Surface1Brush}"
              CornerRadius="20"
              Padding="12,6"
              VerticalAlignment="Center"
              IsVisible="{Binding FavoriteChannels.Count, Converter={StaticResource CountToVisibilityConverter}}">
        <TextBlock Foreground="{DynamicResource TextMutedBrush}"
                   FontSize="13"
                   FontWeight="Medium">
          <Run Text="{Binding FavoriteChannels.Count}" />
          <Run Text=" items" />
        </TextBlock>
      </Border>
    </Grid>

    <TabControl Grid.Row="1"
                IsVisible="{Binding ShowFavoritesEmptyState, Converter={StaticResource InverseBoolConverter}}">
      <TabItem Header="{loc:Translate History.LiveTv}"
               IsVisible="{Binding FavoriteLiveChannels.Count, Converter={StaticResource CountToVisibilityConverter}}">
        <controls:VirtualizedResponsiveGrid ItemsSource="{Binding FavoriteLiveChannels}"
                                            MinItemWidth="280"
                                            MaxColumns="3"
                                            ItemSpacing="16"
                                            ScrollChanged="FavoritesScrollViewer_ScrollChanged">
          <controls:VirtualizedResponsiveGrid.ItemTemplate>
            <DataTemplate x:DataType="models:Channel">
              <controls:MobileLiveTvCard
                  ShowFavoriteMenu="False"
                  ShowRemoveFavoriteMenu="True"
                  Width="{Binding Bounds.Width, RelativeSource={RelativeSource AncestorType=ItemsControl}, Converter={StaticResource ResponsiveCardMetricConverter}, ConverterParameter=liveWidth}" />
            </DataTemplate>
          </controls:VirtualizedResponsiveGrid.ItemTemplate>
        </controls:VirtualizedResponsiveGrid>
      </TabItem>

      <TabItem Header="{loc:Translate History.Series}"
               IsVisible="{Binding FavoriteSeriesItems.Count, Converter={StaticResource CountToVisibilityConverter}}">
        <controls:VirtualizedResponsiveGrid ItemsSource="{Binding FavoriteSeriesItems}"
                                            MinItemWidth="150"
                                            MaxColumns="6"
                                            ItemSpacing="16"
                                            ScrollChanged="FavoritesScrollViewer_ScrollChanged">
          <controls:VirtualizedResponsiveGrid.ItemTemplate>
            <DataTemplate x:DataType="models:Series">
              <controls:MobileSeriesCard
                  ShowRemoveFavoriteMenu="True"
                  Width="{Binding Bounds.Width, RelativeSource={RelativeSource AncestorType=ItemsControl}, Converter={StaticResource ResponsiveCardMetricConverter}, ConverterParameter=posterWidth}"
                  Height="{Binding Bounds.Width, RelativeSource={RelativeSource AncestorType=ItemsControl}, Converter={StaticResource ResponsiveCardMetricConverter}, ConverterParameter=posterHeight}" />
            </DataTemplate>
          </controls:VirtualizedResponsiveGrid.ItemTemplate>
        </controls:VirtualizedResponsiveGrid>
      </TabItem>

      <TabItem Header="{loc:Translate History.Movies}"
               IsVisible="{Binding FavoriteVodChannels.Count, Converter={StaticResource CountToVisibilityConverter}}">
        <controls:VirtualizedResponsiveGrid ItemsSource="{Binding FavoriteVodChannels}"
                                            MinItemWidth="150"
                                            MaxColumns="6"
                                            ItemSpacing="16"
                                            ScrollChanged="FavoritesScrollViewer_ScrollChanged">
          <controls:VirtualizedResponsiveGrid.ItemTemplate>
            <DataTemplate x:DataType="models:Channel">
              <controls:MobileVodCard
                  ShowRemoveFavoriteMenu="True"
                  Width="{Binding Bounds.Width, RelativeSource={RelativeSource AncestorType=ItemsControl}, Converter={StaticResource ResponsiveCardMetricConverter}, ConverterParameter=posterWidth}"
                  Height="{Binding Bounds.Width, RelativeSource={RelativeSource AncestorType=ItemsControl}, Converter={StaticResource ResponsiveCardMetricConverter}, ConverterParameter=posterHeight}" />
            </DataTemplate>
          </controls:VirtualizedResponsiveGrid.ItemTemplate>
        </controls:VirtualizedResponsiveGrid>
      </TabItem>
    </TabControl>

    <controls:MobileEmptyStateView Grid.Row="1"
                                   IsVisible="{Binding ShowFavoritesEmptyState}"
                                   IconKind="HeartOutline"
                                   Title="{loc:Translate Favorites.Empty.Title}"
                                   Description="{loc:Translate Favorites.Empty.Description}" />
  </Grid>
</UserControl>
