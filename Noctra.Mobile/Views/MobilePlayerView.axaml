<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:models="using:Noctra.Models"
             xmlns:vm="clr-namespace:Noctra.ViewModels;assembly=Noctra.Core"
             xmlns:loc="using:Noctra.Mobile.Localization"
             xmlns:controls="using:Noctra.Mobile.Controls"
             xmlns:icons="clr-namespace:Material.Icons.Avalonia;assembly=Material.Icons.Avalonia"
             xmlns:views="using:Noctra.Mobile.Views"
             x:Class="Noctra.Mobile.Views.MobilePlayerView"
             x:Name="PlayerRoot"
             x:DataType="vm:PlayerViewModel"
             x:CompileBindings="False"
             mc:Ignorable="d"
             d:DesignWidth="390"
             d:DesignHeight="260">
  <UserControl.Styles>
    <Style Selector="Button.compactPlayerAction">
      <Setter Property="MinHeight" Value="44" />
      <Setter Property="MinWidth" Value="96" />
      <Setter Property="Padding" Value="10,0" />
    </Style>
  </UserControl.Styles>

  <Grid Background="{DynamicResource PlayerSurfaceBrush}"
        ClipToBounds="True"
        Tapped="OnPlayerBackgroundTapped">
    <!-- Double tap and gesture zones split the visible player area into left/right halves. -->
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="*" />
      <ColumnDefinition Width="*" />
    </Grid.ColumnDefinitions>

    <Grid x:Name="VideoSurfaceLayer"
          Grid.ColumnSpan="2"
          ClipToBounds="True"
          IsVisible="{Binding !IsEpgPanelOpen}">
      <Border x:Name="VideoSurfaceSlot"
              Background="Transparent"
              ClipToBounds="True"
              IsHitTestVisible="False" />

      <!-- Watermark is bound to the same visual layer as the native video slot. -->
      <views:MobileWatermarkView x:Name="MobileWatermark"
                                  ZIndex="10"
                                  HorizontalAlignment="Right"
                                  VerticalAlignment="Bottom"
                                  Margin="0,0,24,120"
                                  IsHitTestVisible="False" />
    </Grid>

    <Border Grid.ColumnSpan="2"
            ZIndex="4"
            Height="130"
            VerticalAlignment="Top"
            IsHitTestVisible="False"
            Background="{DynamicResource PlayerTopGradientBrush}"
            IsVisible="{Binding IsTopOverlayVisible}" />

    <Border Grid.ColumnSpan="2"
            ZIndex="4"
            Height="260"
            VerticalAlignment="Bottom"
            IsHitTestVisible="False"
            Background="{DynamicResource PlayerBottomGradientBrush}"
            IsVisible="{Binding IsTopOverlayVisible}" />

    <views:MobilePlayerTopOverlay Grid.ColumnSpan="2"
                                  ZIndex="35"
                                  VerticalAlignment="Top"
                                  IsVisible="{Binding IsTopOverlayVisible}" />

    <views:MobilePlayerCenterControls Grid.ColumnSpan="2"
                                      ZIndex="34"
                                      HorizontalAlignment="Center"
                                      VerticalAlignment="Center"
                                      IsVisible="{Binding IsBottomControlsVisible}" />

    <!-- Mobile subtitle overlay. Text/style bindings update instantly without restarting the native player. -->
    <Border Grid.ColumnSpan="2"
            ZIndex="9"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Bottom"
            Margin="18,0,18,96"
            Padding="10,6"
            CornerRadius="8"
            Background="{Binding SubtitleBackgroundOpacity, Converter={StaticResource SubtitleBackgroundBrushConverter}}"
            IsHitTestVisible="False"
            IsVisible="{Binding IsMobileSubtitleBottomVisible}">
      <TextBlock Text="{Binding CurrentSubtitleText}"
                 Foreground="{DynamicResource PlayerOverlayTextBrush}"
                 FontSize="{Binding SubtitleFontSize}"
                 FontWeight="SemiBold"
                 TextAlignment="Center"
                 TextWrapping="Wrap"
                 MaxLines="3" />
    </Border>

    <Border Grid.ColumnSpan="2"
            ZIndex="9"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Top"
            Margin="18,72,18,0"
            Padding="10,6"
            CornerRadius="8"
            Background="{Binding SubtitleBackgroundOpacity, Converter={StaticResource SubtitleBackgroundBrushConverter}}"
            IsHitTestVisible="False"
            IsVisible="{Binding IsMobileSubtitleTopVisible}">
      <TextBlock Text="{Binding CurrentSubtitleText}"
                 Foreground="{DynamicResource PlayerOverlayTextBrush}"
                 FontSize="{Binding SubtitleFontSize}"
                 FontWeight="SemiBold"
                 TextAlignment="Center"
                 TextWrapping="Wrap"
                 MaxLines="3" />
    </Border>
    <!-- Left zone: double tap seeks back, vertical drag adjusts brightness, horizontal drag seeks. -->
    <Border x:Name="LeftZone" Grid.Column="0" Background="Transparent"
            DoubleTapped="OnLeftDoubleTapped"
            PointerPressed="OnZonePointerPressed"
            PointerMoved="OnZonePointerMoved"
            PointerReleased="OnZonePointerReleased" />
    <!-- Right zone: double tap seeks forward, vertical drag adjusts volume, horizontal drag seeks. -->
    <Border x:Name="RightZone" Grid.Column="1" Background="Transparent"
            DoubleTapped="OnRightDoubleTapped"
            PointerPressed="OnZonePointerPressed"
            PointerMoved="OnZonePointerMoved"
            PointerReleased="OnZonePointerReleased" />

    <!-- Buffering / loading shield: desktop overlay parity for mobile. -->
    <Border Grid.ColumnSpan="2"
            ZIndex="20"
            Background="{DynamicResource PlayerOverlayShieldBrush}"
            IsHitTestVisible="False"
            IsVisible="{Binding IsBufferShieldVisible}">
      <StackPanel HorizontalAlignment="Center"
                  VerticalAlignment="Center"
                  Spacing="14"
                  MaxWidth="320">
        <Grid Width="64" Height="64" HorizontalAlignment="Center">
          <Arc Width="58" Height="58"
               Stroke="{DynamicResource AccentBrush}"
               StrokeThickness="4"
               StartAngle="0"
               SweepAngle="95"
               VerticalAlignment="Center"
               HorizontalAlignment="Center">
            <Arc.RenderTransform>
              <RotateTransform />
            </Arc.RenderTransform>
            <Arc.Styles>
              <Style Selector="Arc">
                <Style.Animations>
                  <Animation Duration="0:0:1" IterationCount="Infinite">
                    <KeyFrame KeyTime="0:0:0"><Setter Property="RotateTransform.Angle" Value="0" /></KeyFrame>
                    <KeyFrame KeyTime="0:0:1"><Setter Property="RotateTransform.Angle" Value="360" /></KeyFrame>
                  </Animation>
                </Style.Animations>
              </Style>
            </Arc.Styles>
          </Arc>
          <icons:MaterialIcon Kind="ShieldCheckOutline"
                              Width="28" Height="28"
                              Foreground="{DynamicResource PlayerOverlayTextBrush}"
                              HorizontalAlignment="Center"
                              VerticalAlignment="Center" />
        </Grid>
        <TextBlock Text="{Binding PlayerLoadingWarningMessage}"
                   Foreground="{DynamicResource WarningBrush}"
                   FontSize="13"
                   TextWrapping="Wrap"
                   TextAlignment="Center"
                   MaxWidth="300"
                   IsVisible="{Binding PlayerLoadingWarningMessage, Converter={StaticResource StringNotEmptyToVisibilityConverter}}" />
      </StackPanel>
    </Border>

    <!-- Generic overlay message from PlayerOverlayManager. -->
    <Border Grid.ColumnSpan="2"
            ZIndex="42"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            MaxWidth="320"
            Padding="18,12"
            CornerRadius="16"
            Background="{DynamicResource PlayerOverlayStrongBrush}"
            IsHitTestVisible="False"
            IsVisible="{Binding IsOverlayMessageVisible}">
      <StackPanel Orientation="Horizontal" Spacing="10">
        <icons:MaterialIcon Kind="AlertCircleOutline"
                            Width="22" Height="22"
                            Foreground="{DynamicResource AccentBrush}"
                            VerticalAlignment="Center" />
        <TextBlock Text="{Binding OverlayMessage}"
                   Foreground="{DynamicResource PlayerOverlayTextBrush}"
                   FontSize="14"
                   FontWeight="SemiBold"
                   TextWrapping="Wrap"
                   VerticalAlignment="Center" />
      </StackPanel>
    </Border>

    <!-- Volume feedback toast. -->
    <Border Grid.ColumnSpan="2"
            ZIndex="60"
            HorizontalAlignment="Center"
            VerticalAlignment="Top"
            Margin="0,54,0,0"
            Padding="20,12"
            CornerRadius="20"
            Background="{DynamicResource PlayerOverlayStrongBrush}"
            IsHitTestVisible="False"
            IsVisible="{Binding #PlayerRoot.IsVolumeToastVisible}">
      <StackPanel Orientation="Horizontal" Spacing="10">
        <icons:MaterialIcon Kind="VolumeOff"
                            Width="22" Height="22"
                            Foreground="{DynamicResource PlayerOverlayTextBrush}"
                            IsVisible="{Binding IsMuted}" />
        <icons:MaterialIcon Kind="VolumeHigh"
                            Width="22" Height="22"
                            Foreground="{DynamicResource PlayerOverlayTextBrush}"
                            IsVisible="{Binding IsMuted, Converter={StaticResource InverseBoolConverter}}" />
        <TextBlock Text="{loc:Translate Player.Muted}"
                   Foreground="{DynamicResource PlayerOverlayTextBrush}"
                   FontSize="16"
                   FontWeight="Bold"
                   VerticalAlignment="Center"
                   IsVisible="{Binding IsMuted}" />
        <TextBlock Text="{Binding Volume, StringFormat='{}{0}%'}"
                   Foreground="{DynamicResource PlayerOverlayTextBrush}"
                   FontSize="16"
                   FontWeight="Bold"
                   VerticalAlignment="Center"
                   IsVisible="{Binding IsMuted, Converter={StaticResource InverseBoolConverter}}" />
      </StackPanel>
    </Border>

    <!-- Seek / double-tap feedback toast. -->
    <Border Grid.ColumnSpan="2"
            ZIndex="61"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Padding="22,14"
            CornerRadius="20"
            Background="{DynamicResource PlayerOverlayStrongBrush}"
            IsHitTestVisible="False"
            IsVisible="{Binding #PlayerRoot.IsSeekToastVisible}">
      <StackPanel Orientation="Horizontal" Spacing="10">
        <icons:MaterialIcon Kind="{Binding #PlayerRoot.SeekToastIcon}"
                            Width="26" Height="26"
                            Foreground="{DynamicResource AccentBrush}"
                            VerticalAlignment="Center" />
        <TextBlock Text="{Binding #PlayerRoot.SeekToastText}"
                   Foreground="{DynamicResource PlayerOverlayTextBrush}"
                   FontSize="20"
                   FontWeight="Black"
                   VerticalAlignment="Center" />
      </StackPanel>
    </Border>

    <!-- Download feedback toast. -->
    <Border Grid.ColumnSpan="2"
            ZIndex="62"
            HorizontalAlignment="Center"
            VerticalAlignment="Bottom"
            Margin="18,0,18,148"
            Padding="18,12"
            CornerRadius="18"
            Background="{DynamicResource PlayerOverlayStrongBrush}"
            IsHitTestVisible="False"
            IsVisible="{Binding #PlayerRoot.IsDownloadToastVisible}">
      <StackPanel Orientation="Horizontal" Spacing="10">
        <icons:MaterialIcon Kind="DownloadOutline"
                            Width="22" Height="22"
                            Foreground="{DynamicResource AccentBrush}"
                            VerticalAlignment="Center" />
        <TextBlock Text="{Binding DownloadStatusMessage}"
                   Foreground="{DynamicResource PlayerOverlayTextBrush}"
                   FontSize="14"
                   FontWeight="SemiBold"
                   TextWrapping="Wrap"
                   MaxWidth="280"
                   VerticalAlignment="Center" />
      </StackPanel>
    </Border>

    <!-- Gesture feedback for brightness and non-seekable live streams. -->
    <Border Grid.ColumnSpan="2"
            ZIndex="63"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Margin="18"
            Padding="18,12"
            CornerRadius="18"
            Background="{DynamicResource PlayerOverlayStrongBrush}"
            IsHitTestVisible="False"
            IsVisible="{Binding #PlayerRoot.IsGestureToastVisible}">
      <TextBlock Text="{Binding #PlayerRoot.GestureToastText}"
                 Foreground="{DynamicResource PlayerOverlayTextBrush}"
                 FontSize="15"
                 FontWeight="Bold"
                 TextAlignment="Center"
                 TextWrapping="Wrap"
                 MaxWidth="300" />
    </Border>

    <!-- Persistent lock badge remains available while controls are hidden. -->
    <Button Grid.ColumnSpan="2"
            ZIndex="70"
            HorizontalAlignment="Center"
            VerticalAlignment="Top"
            Margin="0,48,0,0"
            Padding="14,8"
            CornerRadius="20"
            Background="{DynamicResource PlayerOverlayStrongBrush}"
            BorderThickness="0"
            IsVisible="{Binding IsLocked}"
            Command="{Binding ToggleLockCommand}">
      <StackPanel Orientation="Horizontal" Spacing="8">
        <icons:MaterialIcon Kind="Lock" Width="18" Height="18" Foreground="{DynamicResource PlayerOverlayTextBrush}" />
        <TextBlock Text="{loc:Translate Player.Mobile.Locked}"
                   Foreground="{DynamicResource PlayerOverlayTextBrush}" FontSize="13" FontWeight="SemiBold" />
      </StackPanel>
    </Button>

    <views:MobilePlayerCompactControls Grid.ColumnSpan="2"
                                       ZIndex="30"
                                       VerticalAlignment="Bottom"
                                       IsVisible="{Binding IsBottomControlsVisible}" />
    <views:MobilePlayerSheets Grid.ColumnSpan="2"
                              ZIndex="50"
                              VerticalAlignment="Bottom"
                              IsVisible="{Binding IsMobileDetailPanelOpen}" />
    <views:MobilePlayerEpgPanel x:Name="EpgPanel"
                                Grid.ColumnSpan="2"
                                ZIndex="40"
                                IsVisible="{Binding IsEpgPanelOpen}" />
  </Grid>

</UserControl>
