<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:Noctra.ViewModels;assembly=Noctra.Core"
             xmlns:loc="using:Noctra.UI.Localization"
             xmlns:controls="using:Noctra.Mobile.Controls"
             xmlns:shared="using:Noctra.UI.Views"
             x:Class="Noctra.Mobile.Views.MobileMyListView"
             x:DataType="vm:MainViewModel"
             x:CompileBindings="False">
  <shared:AdaptiveLibraryView
      Title="{loc:Translate MyList.Title}"
      CountText="{Binding MyList.Count}"
      ShowCountBadge="{Binding MyList.Count, Converter={StaticResource CountToVisibilityConverter}}"
      IsEmpty="{Binding ShowMyListEmptyState}"
      EmptyIconKind="BookmarkOutline"
      EmptyTitle="{loc:Translate MyList.Empty.Title}"
      EmptyDescription="{loc:Translate MyList.Empty.Description}">
    <shared:AdaptiveLibraryView.ItemsHost>
      <controls:MobileSectionedCardFeed Grid.Row="0"
                                        x:Name="PrimaryScrollContent"
                                        ItemContainerTheme="{StaticResource TransparentListBoxItemTheme}">
        <controls:MobileSectionedCardFeed.Sections>
          <controls:MobileCardSection Header="{loc:Translate History.LiveTv}" CardKind="Live" PresentationMode="MyList" SourceItems="{Binding MyListLiveChannels}" />
          <controls:MobileCardSection Header="{loc:Translate History.Series}" CardKind="Series" PresentationMode="MyList" SourceItems="{Binding MyListSeriesItems}" />
          <controls:MobileCardSection Header="{loc:Translate History.Movies}" CardKind="Vod" PresentationMode="MyList" SourceItems="{Binding MyListVodChannels}" />
        </controls:MobileSectionedCardFeed.Sections>
      </controls:MobileSectionedCardFeed>
    </shared:AdaptiveLibraryView.ItemsHost>
  </shared:AdaptiveLibraryView>
</UserControl>
